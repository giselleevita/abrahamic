/**
 * Seed-time wrapper around src/lib/public-demo-policy.ts
 * (Duplicated import path: prisma seed cannot always resolve @/ aliases in all contexts.)
 */

export {
  REMOVED_TRANSLATION_NAMES,
  PUBLIC_DEMO_TRANSLATION_NAMES,
  readerNoteForVerse,
  buildPublicDemoTranslations,
  type DemoTranslation,
} from '../../src/lib/public-demo-policy'
